import React from 'react';
import useAuth from '../hooks/useAuth';

const Admin = () => {
    // const {token} = useAuth();
  return (
    <>
    <h1>Admin</h1>
    <div>token is </div>
    </>
  )
}

export default Admin;